import * as THREE from 'three';
const A=THREE.Group.prototype.add;let G=null;
THREE.Group.prototype.add=function(...os){for(const o of os)if(o?.name==='CONTINUOUS_SLEEVE_V36')G=this;return A.apply(this,os)};
await import('./gift-set-v36.js?build=44');THREE.Group.prototype.add=A;
if(G){G.children.forEach(o=>{if(!o?.isMesh)return;o.geometry?.computeBoundingBox?.();const b=o.geometry?.boundingBox;if(!b)return;const s=new THREE.Vector3();b.getSize(s);if(Math.abs(s.z)<.15&&o.position.y>20.4&&Math.abs(Math.abs(o.rotation.x)-Math.PI/2)<.05)o.visible=false;});
new THREE.TextureLoader().load('../assets/twyne-wordmark-tight-black.svg?v=44',t=>{t.colorSpace=THREE.SRGBColorSpace;t.minFilter=t.magFilter=THREE.LinearFilter;t.generateMipmaps=false;const m=new THREE.Mesh(new THREE.PlaneGeometry(96,10.9),new THREE.MeshBasicMaterial({map:t,transparent:true,opacity:.68,depthTest:false,depthWrite:false,toneMapped:false,side:THREE.DoubleSide}));m.rotation.x=-Math.PI/2;m.position.set(0,20.98,-15.5);m.renderOrder=60000;G.add(m);});
function addText(text,w,z,fs,k){const p=document.createElement('canvas').getContext('2d');p.font=`600 ${fs}px Manrope,Arial`;const a=[...text],ws=a.map(c=>p.measureText(c).width),tot=ws.reduce((x,y)=>x+y,0)+k*(a.length-1),c=document.createElement('canvas');c.width=Math.ceil(tot+160);c.height=320;const x=c.getContext('2d');x.font=`600 ${fs}px Manrope,Arial`;x.fillStyle='#11110f';x.textBaseline='middle';let q=80;a.forEach((ch,i)=>{x.fillText(ch,q,160);q+=ws[i]+k});const t=new THREE.CanvasTexture(c);t.colorSpace=THREE.SRGBColorSpace;t.minFilter=t.magFilter=THREE.LinearFilter;t.generateMipmaps=false;const h=w/(c.width/c.height),m=new THREE.Mesh(new THREE.PlaneGeometry(w,h),new THREE.MeshBasicMaterial({map:t,transparent:true,depthTest:false,depthWrite:false,toneMapped:false}));m.rotation.x=-Math.PI/2;m.position.set(0,21,z);m.renderOrder=60010;G.add(m)}
addText('VOLUME I - KENOPSIA',91,35,112,18);addText('EAU DE PARFUM',58,41.5,92,8);}
